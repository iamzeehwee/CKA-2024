apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-app
  template:
    metadata:
      labels: 
        app: nginx-app
    spec:
      containers:
      - name: nginxpod
        image: nginx
